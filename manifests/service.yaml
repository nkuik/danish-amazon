apiVersion: v1
kind: Service
metadata:
  name: slack-client
  namespace: slack-client
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: LoadBalancer
  selector:
    app: slack-client
